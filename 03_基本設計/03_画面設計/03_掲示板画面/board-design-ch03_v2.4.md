# 第3章: 掲示板TOP一覧画面（BoardTop）

**掲示板機能 基本設計書 v2.4（HarmoNet準拠）**

[← 第2章](board-design-ch02_latest.md) | [目次に戻る](board-design-ch00-index_latest.md) | [第4章 →](board-design-ch04_latest.md)

---

## 🆕 ChangeLog (v2.4 / 2025-11-21)

* 文書レイヤーを「基本設計」に統一（実装寄りのHTML/アニメーション記述を整理）
* HOME/ch02 との整合のため、タブ構成（ALL/NOTICE/RULES）と範囲（GLOBAL/GROUP）の役割を再定義
* カード構造を BoardTop 共通の「掲示板投稿サマリカード」として定義
* 無限スクロール前提を削除し、「ページネーション + 件数セレクタ」前提に変更
* Phase 2 など段階表現を廃止し、「将来拡張」として検索・高度フィルタを整理
* モデレーション／翻訳は本章では扱わず、ch06 の共通仕様に委譲

---

## 3.1 画面概要

BoardTop（掲示板TOP一覧画面）は、HarmoNet の掲示板に投稿された記事を一覧で確認する中核画面である。

* パス: `/board`
* 役割:

  * テナント内の掲示板投稿を、タブ（ALL / NOTICE / RULES）と範囲（GLOBAL / GROUP）で切り替えながら一覧表示する。
  * HOME で気になった投稿を深掘りしたり、過去の投稿をまとめて確認する入口となる。
* 前提:

  * ログインセッションの `tenant_id` に基づき、当該テナントの投稿のみを表示する。
  * 他テナントの投稿は一切表示・検索対象としない（マルチテナント分離）。

本章では、BoardTop の UI 構造と振る舞い（カード構造・タブ／範囲・ソート・ページネーション）を定義し、
投稿作成／モデレーション／翻訳などの共通ロジックは ch05〜ch06 に委ねる。

---

## 3.2 画面構成（レイアウト）

### 3.2.1 構成要素

BoardTop 画面は、次の UI 要素で構成する。

1. ページヘッダー

   * 画面タイトル: 「掲示板」
   * 必要に応じて簡易説明テキスト（例: 「お知らせやルール、住民のみなさんの投稿を一覧で確認できます」）。

2. タブバー（カテゴリ切替）

   * ALL / NOTICE / RULES の 3 タブ。
   * 現在選択中タブを視覚的に強調（色・下線など）。

3. 範囲フィルタ

   * GLOBAL / GROUP など、掲示板の対象範囲を切り替えるセレクタ（トグルまたはセレクトボックス）。
   * ユーザの所属情報に応じて選択可能な範囲を制御する。

4. 投稿サマリカード一覧

   * 各投稿を 1 カードとして縦方向に並べた一覧領域。
   * ページネーション・ソート順の指定結果を反映して表示する。

5. ページネーション + 件数セレクタ

   * 一覧下部にページ切替（例: 前へ／次へ、ページ番号）と 1 ページあたり表示件数（例: 10 / 20 / 50 件）を配置する。

6. 共通フッター

   * アプリ共通のナビゲーション（HOME／掲示板／施設予約 等）。

### 3.2.2 UI トーン

* HOME と同じ Apple カタログ風のミニマルなカード UI を採用。
* 背景は白基調、カードは角丸 2xl・ごく薄いシャドウ・控えめな境界線のみ。
* 強い色はタブのアクティブ状態・重要バッジなど「必要最小限」に限定し、落ち着いた印象を優先する。

---

## 3.3 投稿サマリカード構造

BoardTop では、すべての投稿を次の「サマリカード」構造で表示する。詳細内容（本文全文／PDFプレビュー／コメント等）は詳細画面（/board/[id]）側で扱う。

### 3.3.1 カード内情報構造

| 項目         | 必須 | 内容・例                                                |
| ---------- | -- | --------------------------------------------------- |
| タグバッジ      | ○  | NOTICE / RULES / その他カテゴリを示す小さなラベル（アイコン + テキスト）      |
| タイトル       | ○  | 投稿タイトル。最大 50 文字程度を表示し、それ以上は末尾を `…` で省略              |
| メタ情報行      | ○  | 投稿日時（例: 2025/11/21 10:30）、投稿者種別（管理組合 / 一般住民）などの簡易情報 |
| 本文サマリ      | ○  | 本文冒頭を最大 2〜3 行程度に収まるよう省略表示（改行はスペースに畳み込み）             |
| クリップアイコン   | △  | 添付ファイルがある場合のみ表示するアイコン。PDF / 画像など種別は詳細画面で確認。         |
| ピン留めインジケータ | △  | 管理者が「重要」指定した投稿に表示するピンマーク（タブ種別に応じて利用）。               |
| 回覧板ステータス   | △  | 回覧板種別の投稿にのみ、「未読」「既読」ステータスバッジを表示。                    |

### 3.3.2 カードの動作

* カード全体がタップ領域となり、タップ時に `/board/[id]` へ遷移する。
* カード内の個別要素（タグバッジ等）は、基本的には追加アクションを持たず、「一覧としての読みやすさ」を優先する。
* アクセシビリティ配慮として、カードには `aria-label` 等でタイトル・カテゴリ・投稿日を読み上げ可能な形で付与する（詳細は別途アクセシビリティ方針で定義）。

---

## 3.4 タブ（ALL / NOTICE / RULES）の仕様

### 3.4.1 タブの役割

* **ALL タブ**

  * NOTICE + RULES + その他すべての投稿を含むビュー。
  * デフォルト表示とし、「掲示板の全体的な流れ」を把握する用途を想定する。

- **NOTICE タブ**
  - 対象: 管理組合／管理会社ロールが投稿した「お知らせ」カテゴリの投稿。
  - 用途: 重要連絡やイベント案内など、住民が見落としたくない情報を集中表示する。
  - 回覧板（必読レベルの周知事項）は、管理投稿の中で `is_circulation = true` として扱われ、
    同じく NOTICE タブ内に表示される（既読管理の対象にもなる）。

> 補足: 「お知らせ」専用の別機能は設けず、管理組合ロールの掲示板投稿（NOTICE）を「お知らせ」として扱う。

* **RULES タブ**

  * 管理規約・利用ルール・マナーなど、長期的に参照される投稿を対象とする。
  * 新着順だけでなくピン留めを活用し、「いつでも同じ場所にある」ことを重視する。

### 3.4.2 タブ切替時の振る舞い

* タブをタップすると、現在の範囲フィルタ・ソート条件・ページ番号を初期状態にリセットして再検索する。

  * 例: ALL → NOTICE に切り替えた際は、ページ番号を 1 ページ目・デフォルトソートに戻す。
* タブ状態は URL クエリ（例: `/board?tab=notice`）に反映し、HOME からの遷移やブックマークとの整合を取る。

---

## 3.5 範囲フィルタ（GLOBAL / GROUP）

### 3.5.1 範囲の定義

* **GLOBAL**

  * テナント全体に対する共通掲示板（全住民が閲覧対象）。
  * 主に管理組合・管理会社からの通知や、全体向けのお知らせを想定する。

* **GROUP**

  * 特定のグループ（棟／フロア／サークル等）に属する住民のみが閲覧対象の掲示板。
  * ユーザは 0〜複数のグループに所属し、所属グループの投稿のみが対象となる。

### 3.5.2 BoardTop での表示ルール

* デフォルトでは「GLOBAL + 所属グループ」をまとめて表示する（ALL タブの全体像を把握しやすくするため）。
* 範囲フィルタ UI では、次のような選択肢例を提供する。

  * すべて（GLOBAL + 所属グループ）
  * 全体のみ（GLOBAL）
  * グループのみ（GROUP）
* ユーザが所属していないグループの投稿は、いかなる選択状態でも表示されない（RLS によるアクセス制御前提）。

---

## 3.6 ソート・ページネーション

### 3.6.1 ソート順

BoardTop でのソートは、次の優先順位で定義する。

1. ピン留め（重要投稿）

   * 管理者が「ピン留め」指定した投稿を、常に一覧上位に表示する。
   * ピン留め投稿同士の順序は `pinned_at` の降順（新しくピン留めされたものが上）。

2. 投稿日時

   * 非ピン留め投稿については `created_at` の降順（新しい投稿が上）で表示する。

必要に応じて、将来的に「古い順」「更新日順」などのソートオプションを追加できるよう、UI 上はソート条件セレクタ（ドロップダウン等）を配置することを検討する（MVPでは固定でもよい）。

### 3.6.2 ページネーションと件数セレクタ

* 1ページあたりの表示件数はデフォルト 20 件とし、ユーザが 10 / 20 / 50 件から選択可能とする。
* ページネーション UI では、次の要素を基本とする。

  * 現在ページ番号
  * 前へ／次へボタン
  * 必要に応じて先頭／末尾へのショートカット
* ページ切替時は、現在のタブ・範囲・ソート条件を維持したままページ番号のみ変更する。
* URL クエリ（例: `page=2&perPage=20`）に反映し、リロードや共有時にも状態を再現可能とする。

---

## 3.7 空状態とエラー状態

### 3.7.1 空状態（該当投稿なし）

現在のタブ・範囲・フィルタ条件で投稿が存在しない場合、一覧領域に次のようなメッセージを表示する。

* メインメッセージ（例）:

  * 「該当する投稿はありません。」
* サブメッセージ（例）:

  * ALL タブ: 「条件を変更するか、新しい投稿をお待ちください。」
  * NOTICE / RULES タブ: 「管理組合からのお知らせ・ルールは、決まり次第ここに表示されます。」

必要に応じて、「新規投稿」ボタンをあわせて表示するが、表示可否・位置は権限や画面共通ポリシーに従う（詳細は投稿フォーム ch05 で定義）。

### 3.7.2 エラー状態（取得失敗）

投稿一覧の取得に失敗した場合は、次のような UI メッセージを想定する（文言は i18n 設計に従う）。

* メインメッセージ（例）:

  * 「投稿一覧を読み込めませんでした。」
* アクション:

  * 「再読み込み」ボタン
  * ネットワークエラー等、ユーザが操作可能な範囲に説明をとどめる。

詳細なメッセージキー・表示位置は、共通メッセージ仕様（別紙）と合わせて決定する。

---

## 3.8 将来拡張（検索・高度フィルタ）

MVP 時点では BoardTop における検索・高度なフィルタリングは必須としないが、将来的な拡張候補として次を想定する。

* キーワード検索（タイトル・本文の部分一致）
* 期間フィルタ（投稿日 From/To）
* カテゴリ複数選択フィルタ
* 回覧板の既読／未読フィルタ

これらは UI 上は 1 つの「検索・絞り込みパネル」にまとめ、必要になった時点で詳細設計・実装を行う。

---

## 3.9 設計意図まとめ

1. **読みやすさ優先のサマリカード**

   * BoardTop は「読む体験」に特化し、1 投稿 = 1 カードのシンプルな一覧構造を採用する。

2. **テナントスコープの自然な分離**

   * `tenant_id` によるデータ分離を前提としつつ、UI 上は「自分のコミュニティだけが見える」自然な体験にする。

3. **HOME・詳細画面との役割分担**

   * HOME は「ごく一部の代表投稿」、BoardTop は「全体を俯瞰」、詳細画面は「内容をじっくり読む」という役割を明確に分ける。

4. **拡張しやすい枠組み**

   * タブ・範囲・ソート・ページネーションを素直な形で定義し、将来的な検索・高度フィルタ・AI連携を ch06 側で拡張しやすいようにしている。

---

## GPTレビュー要約コメント（更新版）

> 第3章は、掲示板TOPの「読む体験」と HOME / 詳細画面との境界を明確にした基本設計になっている。
> 旧版で混在していた実装詳細や Phase 記述を整理し、タブ／範囲／ソート／ページネーションという UI 軸に集約したことで、
> 今後の詳細設計・実装が進めやすい骨組みになっている。
> マルチテナント構造や回覧板ステータスも「カードのメタ情報」として自然に溶け込んでおり、
> UI トーン（やさしく・自然・控えめ）との整合も保たれている。
> — *GPT上流設計レビュー担当（再編集）*

---

**Document ID:** SEC-APP-BOARD-DESIGN-002-CH03
**Version:** 2.4
**Created:** 2025-10-27
**Last Updated:** 2025-11-21
**Author:** TKD + GPT (HarmoNet PMO)
